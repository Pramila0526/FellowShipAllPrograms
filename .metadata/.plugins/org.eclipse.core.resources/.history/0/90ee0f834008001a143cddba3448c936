package com.bridgelabz.fellowshipprogramss.datastructure;

import java.io.*;
import java.util.Arrays;
import java.util.LinkedList;
import java.util.Scanner;

import com.bridgelabz.fellowshipprogramss.utility.DataStructure; 

public class FileIO 
{ 
  public static void main(String[] args)throws Exception
  { 
  // We need to provide file path as the parameter: 
  // double backquote is to avoid compiler interpret words 
  // like \test as \t (ie. as a escape sequence) 
 File file = new File("/home/admin94/Desktop/Text"); 
  
  BufferedReader br = new BufferedReader(new FileReader(file)); 
  String fileWords; 
    while ((fileWords = br.readLine()) != null) 
    {
    System.out.println(fileWords); 
    fileWords+=br.readLine();
    String data[]=fileWords.split(" ");
    System.out.println(Arrays.toString(data));	 
    
	 DataStructure list = new DataStructure();
			
		 Scanner sc=new Scanner(System.in); 
		 System.out.println("Enter the Content");
			
		 String fileContent=sc.next(); 
		 
	   BufferedWriter writer = new BufferedWriter(new FileWriter(file));
	   writer.write( DataStructure.LinkedList.search(fileContent));
	   DataStructure.LinkedList.insert(fileContent);
	     DataStructure.LinkedList.display();
	     writer.write(DataStructure.LinkedList.search(data));
	   
	   
			
    }	
   }
  }